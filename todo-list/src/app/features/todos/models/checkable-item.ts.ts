/**
 * создает интерфейс CheckableItem как кастомный тип днных без реализации .
 *
 * @property  {boolean} checked указывает выбрана ли задача.
 * @property  {any} data данные .
 */

export interface CheckableItem {
  checked: boolean;
  data: any;
}
